import Section from "./components/Section";
import Markdown from "react-markdown";

export default function Home() {
  return (
    <div className="w-full h-screen bg-red-50 flex flex-col items-center justify-center">
      <h1>hi</h1>
    </div>
  );
}
